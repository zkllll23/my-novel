# 项目配置
my-novel:
  # 跨域配置
  cors:
    # 允许跨域的域名
    allow-origins:
      - http://localhost:1024
      - http://localhost:8080
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/mynovel?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456